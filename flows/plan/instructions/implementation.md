# Implementation

Execute the plan. Follow the implementation steps in order, checking each off as you go.

## What to do

1. Read the plan and create a Task for each checkbox item using the Task tool
2. Work through tasks in order — complete each task before moving to the next
3. When a task is done, mark the corresponding checkbox in `plan.md` as checked (`- [x]`) and mark the Task as complete
4. Keep changes minimal and focused — do not refactor beyond what the plan specifies
5. If you hit an unexpected blocker, note it on the task and continue with unblocked tasks

## Output

This step produces working code. No document is generated — complete the step when all tasks are done and all checkboxes in `plan.md` are marked.
